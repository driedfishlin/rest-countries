//Limit on the number of pages
export const itemsLimit = 25;

export const fuzzySearchOptions = {
	threshold: 0.4,
	shouldSort: true,
	keys: ['name', 'nativeName', 'alpha2Code', 'alpha3Code', 'altSpellings'],
};
